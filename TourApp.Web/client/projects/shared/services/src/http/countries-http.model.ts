export type Countries = {
    data: {
        name: string,
        dial_code: string
    }[]
};

export type States = {
    data: {
        states: {
            name: string
        }[]
    }
}

export type Cities = {
    data: string[]
}
